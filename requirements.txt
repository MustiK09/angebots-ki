# Angebots-KI – Abhängigkeiten
# Empfehlung: stets die aktuelle anthropic-Version verwenden (pip install -U anthropic),
# da neue Modelle (claude-opus-4-8) und strukturierte Outputs ein aktuelles SDK benötigen.
anthropic>=0.49
streamlit>=1.32
reportlab>=4.0
pypdf>=4.0
python-dotenv>=1.0
pandas>=2.0
boto3>=1.34          # optional: Produktdaten/Vorlagen aus AWS S3 laden
